# Basic Example

Create a **GSheetsCommander/Google Sheets Config** asset, configure it for your deployed Apps Script endpoint, and assign it to `BasicExample` in the Inspector. The component demonstrates getting and setting a cell, reading a range, and appending a row.
